; Type numbers come from the internal catalog. Map and component are always
; normalized to map=auto so the incorporated deposits plugin assigns channels.
;
; [simple_ore]
; token = $TYPE_MINE_SIMPLE
; radius = ore
; icon = simple_parts
; minimap = 1
; editor = simple
